# scheduled-batch-job

Skeleton multicloud switcher module.

## TODO

- Add an explicit provider switch for AWS and GCP.
- Delegate cloud resources to provider-specific wrapper modules.
- Keep AWS and GCP behavior namespaced under `modules/aws` and `modules/gcp`.
