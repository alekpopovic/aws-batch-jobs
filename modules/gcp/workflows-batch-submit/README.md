# workflows-batch-submit

Skeleton module for Google Workflows submitting GCP Batch jobs.

## TODO

- Implement a Workflow that submits a GCP Batch job.
- Keep the flow equivalent to the AWS scheduler -> workflow -> batch pattern.
- Do not introduce Cloud Functions or Cloud Run jobs.
