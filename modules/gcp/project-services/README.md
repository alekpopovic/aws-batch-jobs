# project-services

Skeleton module for enabling Google APIs required by the GCP scheduled Batch job stack.

## TODO

- Enable Batch, Cloud Scheduler, Workflows, and Logging APIs.
- Keep project ID configurable through `var.project_id`.
